export const metadata = {
  title: 'Metrics Dashboard | Grewal Shopfront & Shutters',
  robots: { index: false, follow: false },
};

export default function MetricsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
